// Legacy vocabulary generators were retired after the quality rebuild.
// Keep these no-op exports only so old imports do not fail if any stale file references remain.

export function createLevelSlots() {
  return [];
}

export function generateLevelVocabulary() {
  return [];
}
